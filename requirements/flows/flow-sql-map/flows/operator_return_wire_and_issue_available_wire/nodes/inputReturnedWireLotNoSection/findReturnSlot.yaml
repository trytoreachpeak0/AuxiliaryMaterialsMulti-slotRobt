subgraph_id: inputReturnedWireLotNoSection
node_id: findReturnSlot
node_text: 查询可用于存放归还焊丝的空闲格口
access_type: application_db
catalog_status: pending_catalog
sql_ids:
  - app.slot.find_return_slot
business_meaning: >-
  批号查询阶段预分配归还格口（与 find_available 预留的最大 index idle 格一致），供后续 openReturnSlot 使用。
output_fields:
  - name: return_slot_id
    source_type: sql_catalog
    source_ref: app.slot.find_return_slot
    catalog_field: return_slot_id
    source_text: 归还焊丝目标格口 ID。
  - name: return_slot_no
    source_type: sql_catalog
    source_ref: app.slot.find_return_slot
    catalog_field: return_slot_no
    source_text: 归还焊丝目标格口编号。
outcome_routing:
  single:
    next: queryWireReturnedWeightBySpec
  empty:
    next: showNoReturnSlotAvailableMessage
