# scenarios — 验证场景

每个流程的正常路径与关键异常分支的输入、操作与期望。可在工具里选流程 →「开始」→ 填输入 →「单步 / 连续执行」复现；异常分支通过修改输入或 MES 设置触发。

种子数据要点：操作员 `1001=张三`；归还焊丝批号 `14Z1888-0160`(φ42,归还)；可用焊丝批号 `14Z1888-0888`(φ42,可用，绑定在 A05)；机台 `1QH029`；格口 A01-A03 可用空闲、A04/A08 归还空闲、A05/A07 含可用焊丝、A06 含归还焊丝。

详见各流程文件：

- `operator_return_and_issue.md`
- `material_handler_load.md`
- `open_slots.md`
